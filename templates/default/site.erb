server {
    listen <%= node['aws_elb_webserver']['port'] %>;
    server_name <%= node['hostname'] %>;

    location / {
        proxy_set_header   X-Forwarded-For $remote_addr;
        proxy_set_header   Host $http_host;
        proxy_pass         "http://<%= node['hostname'] %>:8081";
    }
}
